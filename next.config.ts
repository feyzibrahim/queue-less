import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['joselyn-thicketed-unsyntactically.ngrok-free.dev'],
};

export default nextConfig;
